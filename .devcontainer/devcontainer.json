{
    "build": {
        // Path is relataive to the devcontainer.json file.
        "dockerfile": "../Containerfile"
    },
    "customizations": {
        "vscode": {
            "settings": {},
            "extensions": [
            ]
        }
    }
}